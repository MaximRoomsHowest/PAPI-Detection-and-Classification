# Apps

Runnable application surfaces live here.

| Path | Purpose |
|---|---|
| `backend/` | FastAPI API for uploads, inference, result logs, and media exports. |
| `frontend/` | Vite/React demo UI with Backend API mode and Mock mode. |

Both apps depend on shared project assets at the repo root, especially `models/`,
`configs/`, and `test_videos/`.
