# Development / test dependencies, layered on top of the runtime set.
#
# CI and local test runs install this file; the production Docker image installs
# only requirements.txt, so pytest/httpx2 never ship in the container (audit DCK-D5).
-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0
# httpx2 backs FastAPI's TestClient (Starlette). Test-only — no runtime import.
httpx2==2.12.0
